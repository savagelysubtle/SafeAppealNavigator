{
  "agent_id": "ceo_agent_test",
  "agent_name": "CEOAgent",
  "agent_type": "Interface",
  "description": "Test version of CEO Agent using test model - no API keys required",
  "a2a_url_template": "http://{host}:{port}",
  "version": "1.0.0-test",
  "llm_provider": "test",
  "llm_model_name": "test",
  "skills": [
    {
      "name": "handle_user_request",
      "description": "Takes a raw user request, formulates a task for the orchestrator, and returns the final result.",
      "input_schema": {
        "type": "object",
        "properties": {
          "user_prompt": {
            "type": "string"
          }
        },
        "required": ["user_prompt"]
      },
      "output_schema": {
        "type": "string"
      }
    }
  ],
  "dependencies": [],
  "tags": ["interface", "ceo", "user_facing", "test"]
}
